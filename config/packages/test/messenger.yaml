framework:
    messenger:
        transports:
            # zenstruck/messenger-test transport: queued messages are inspectable
            # in tests via $this->transport('async') and never hit Redis.
            async: 'test://'
